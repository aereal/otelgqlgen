---
schema:
  - '*.gql'
exec:
  layout: follow-schema
  dir: ./internal/execschema
  package: execschema
model:
  filename: ./internal/model/model_gen.go
  package: model
resolver:
  layout: follow-schema
  dir: ./internal/resolvers
  package: resolvers
  filename_template: "{name}.resolvers.go"

