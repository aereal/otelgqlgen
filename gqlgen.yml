---
schema:
  - '*.gql'
exec:
  layout: follow-schema
  dir: ./test/execschema
  package: execschema
model:
  filename: ./test/model/model_gen.go
  package: model
resolver:
  layout: follow-schema
  dir: ./test/resolvers
  package: resolvers
  filename_template: "{name}.resolvers.go"

