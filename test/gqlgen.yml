---
schema:
  - '*.gql'
exec:
  layout: follow-schema
  dir: ./execschema
  package: execschema
model:
  filename: ./model/model_gen.go
  package: model
resolver:
  layout: follow-schema
  dir: ./resolvers
  package: resolvers
  filename_template: "{name}.resolvers.go"

